# Codex レビュー

`crates/` 配下のコードを変更してコミットする前に、Codex CLI が利用可能であれば `/codex-cli` スキルを使ってコードレビューを依頼する。

## 発火条件

- `crates/` 配下のファイルに変更がある場合
- docs のみの変更では不要

## 手順

1. `git diff --cached` または `git diff` で変更内容を確認
2. `/codex-cli` スキルを呼び出し、変更箇所のレビューを依頼
3. レビュー指摘があれば修正してからコミット
