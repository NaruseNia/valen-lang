//! Emit `.class` for Valen `class` / `data class` definitions.

pub fn emit_class() {
    todo!()
}

pub fn emit_data_class() {
    todo!("auto-generate equals / hashCode / toString / copy for data class")
}
