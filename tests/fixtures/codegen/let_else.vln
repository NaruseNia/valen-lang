package com.example;

pub enum Color {
    Red,
    Green,
    Blue(value: Int),
}

pub class LetElseTest {
    fn extractBlue(self, c: Color) -> Int {
        let Color::Blue(value) = c else { return 0; };
        value
    }
}
