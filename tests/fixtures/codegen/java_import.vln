package com.example;

import java.util.ArrayList;
import java.io.File as JavaFile;

pub class Importer {
    fn createList(self) -> ArrayList {
        ArrayList()
    }

    fn createFile(self, path: String) -> JavaFile {
        JavaFile(path)
    }
}
