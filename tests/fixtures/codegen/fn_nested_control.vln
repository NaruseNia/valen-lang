package com.example;

pub class Nested {
    fn classify(self, x: Int) -> String {
        if x > 0 {
            if x > 100 {
                "big"
            } else {
                "small"
            }
        } else {
            "negative"
        }
    }
}
